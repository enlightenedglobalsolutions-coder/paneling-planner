# Archived variants — kept on disk, never shipped to Pages.
_old/

# Backups written by egs-deploy.sh
index.html.bak.*

# Scratch theme preview — judged locally, never shipped to Pages.
preview.html

# Output of render_svg.js — regenerate it, don't commit it.
render-*.svg
render-*.svg.png

.DS_Store
